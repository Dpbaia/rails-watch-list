<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%# <%= f.association :list, as: :radio_buttons %>
  <%= f.submit "Create new review", class: "btn btn-primary" %>
<% end %>
