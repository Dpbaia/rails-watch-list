<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%# <%= f.association :list, as: :radio_buttons %>
  <%= f.submit "Add new movie to #{@list.name}", class: "btn btn-primary" %>
<% end %>
